# Core dependencies for YOLO-based traffic monitoring
ultralytics>=8.0.0        # YOLOv8/YOLOv11
opencv-python>=4.8.0      # Image processing and video capture (includes FFmpeg for HLS streams)
numpy>=1.24.0             # Numerical operations
torch>=2.0.0              # PyTorch (required by YOLOv8)
torchvision>=0.15.0       # Vision utilities for PyTorch

# DeepSORT tracking (pipeline.py)
deep-sort-realtime>=1.3.2 # DeepSORT multi-object tracker with built-in Re-ID

# Utilities
pillow>=10.0.0           # Image handling
python-dateutil>=2.8.2   # Date/time utilities

# FastAPI service (app/)
fastapi>=0.110.0          # Web framework
uvicorn[standard]>=0.29.0 # ASGI server (use --workers 1; YOLO model is not fork-safe)
pydantic>=2.0.0           # Data validation (also used by FastAPI)
pydantic-settings>=2.0.0  # BaseSettings for environment-driven config
python-multipart>=0.0.9   # Required for file-upload support via FastAPI
httpx>=0.27.0             # Async HTTP client (optional; useful for integration tests)

# Optional but recommended
# matplotlib>=3.7.0       # For visualizations
# pandas>=2.0.0          # Data analysis
# pyyaml>=6.0            # Configuration files

